import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.zimboFit',
  appName: 'gym-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
