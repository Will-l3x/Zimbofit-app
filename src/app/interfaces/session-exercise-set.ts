/* eslint-disable @typescript-eslint/naming-convention */
export interface SessionExerciseSet {
    exercise_id: string;
    workout_id: string;
    // reps?: Number;
    // rest?: Number;
    // weight?: Number;
    // distance?: Number;
    // time: Number;
    completed?: boolean;
    measurements?: any[];
}
